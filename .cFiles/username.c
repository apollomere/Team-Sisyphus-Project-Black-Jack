#include<stdio.h>
#include "username.h"
#include<string.h>
void register_name(pn pnData){
        printf("\n%s is the chosen name",pnData.name);// Output users chosen name.
        printf("\nPress ENTER to continue, or BACKSPACE to go back.");
                                        /*Extra option if user is satisfied with name or not. 
                                                if No: restart the function, 
                                                if Yes, ensure name is solidified, and end function
                                                Will probably try to incorporate true/false values, so going to have to look into that-Andre*/
}
